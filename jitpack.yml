before_install:
  - sdk list java
  - sdk install java 25-tem
  - sdk use java 25-tem
jdk:
  - openjdk21
install:
  - ./gradlew --version
  - ./gradlew publishReleasePublicationToMavenLocal --stacktrace --debug